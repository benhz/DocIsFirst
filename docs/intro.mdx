import ProfileCard from '@site/src/components/ProfileCard';
import TeamWall from '@site/src/components/TeamWall';
import MySpace from '@site/src/components/MySpace';
import Tip from '@site/src/components/Tip';
import Carousel from '@site/src/components/Carousel';
import { teamMembers, getFullStackMembers, getUIMembers, getFrontendMembers } from '@site/src/config/teamMembers';

# 🏠 欢迎来到开发者中心
—— 这里是我们 5 个人的小社会，但写出的代码能镇压一整个星系

<Carousel images={[
  "/img/logo.svg",
  "/img/logo.svg",
  "/img/logo.svg",
]} />

<ProfileCard
  name="你的名字"
  role="全栈大魔王 / 能写能打 / 偶尔上线背锅"
  slogan="我不生产 Bug，我只是 Bug 的搬运工"
/>

这里不是普通的文档中心。  
这里是——  

**我们 5 人小队的武功秘籍、避雷宝典、生存指南（劝退手册）。**

我们虽然没有 50 人团队那么多的背锅侠，  
但我们有 **骚话 + 代码 + 抗压能力** 三件套，天下无敌。

---

## 🤣 你将会在这里看到：

<Tip type="fun" message="友情提示：以下内容过度真实，如有雷同，绝非巧合。" />

---

## 📚 产品文档  
—— 看完后你终于知道我们在做啥

### 🟦 [产品文档](./product/index.md)
- 产品到底是啥？为啥做它？  
- 需求从哪来？又往哪去？  
- 为什么功能做完了用户还不会用？

---

## 🧩 平台与通用能力  
—— “权限是谁管的？” 请别再问

### 🟧 [平台能力](./platform/overview.md)
账号、权限、消息这些东西：  
你以为它不重要……  
但每次登录不了的人都会第一时间 @ 你。

---

## 🧱 开发与架构  
—— 三位全栈用生命换来的经验

### 🟥 [开发与架构](./development/overview.md)
- 我们的系统怎么没崩？（惊讶）  
- 代码规范：写给别人，也是写给未来的自己  
- 本地环境搭建：避免你第一天就想离职

---

## 🚒 运维与事故处理  
—— “线上炸了怎么办？” 我们写了手册，你看不看是你的事

### 🟩 [运维手册](./operations/overview.md)
- 上线流程  
- 回滚流程  
- 出事时的心理建设指南  

---

## 🔌 API 文档  
—— 你要找的接口都在这，别再 GitHub 全局搜索了

### 🟨 [API 文档](./api/overview.md)

---

## 👥 团队协作流程  
—— 小团队也要有大团队气质（至少文档得像）

### 🟪 [协作流程](./org/overview.md)

---

## 🛠️ 项目手册  
—— 这里存着所有项目的血泪史

### 🟫 [项目手册](./projects/index.md)

---

# 🌟 团队专属内容  
（骚话 + 真实 + 各种没必要但很好玩的部分）

## 👤 **个人档案区：我们的灵魂展示**

<Tip type="info" message="以下信息为团队灵魂核心，如需更新请保持骚话密度。" />

### 🧑‍💻 全栈天团（3 人）
{getFullStackMembers().map((member) => (
  <ProfileCard
    key={member.id}
    name={member.name}
    avatar={member.avatar}
    role={member.role}
    slogan={member.slogan}
  />
))}

---

### 🎨 UI / 设计部（1 人）
{getUIMembers().map((member) => (
  <ProfileCard
    key={member.id}
    name={member.name}
    avatar={member.avatar}
    role={member.role}
    slogan={member.slogan}
  />
))}

---

### 🕸️ 前端部（1 人）
{getFrontendMembers().map((member) => (
  <ProfileCard
    key={member.id}
    name={member.name}
    avatar={member.avatar}
    role={member.role}
    slogan={member.slogan}
  />
))}

---

# 🧱 团队墙组件 TeamWall
—— 把团队的灵魂刻在文档首页

<TeamWall
  members={teamMembers.map((member) => ({
    name: member.name,
    avatar: member.avatar,
    role: member.role,
    quote: member.quote,
  }))}
/>

---

# 🧘 **每个人的私人空间：My Space 模块**

<Tip type="star" message="这是你们自由发挥、写骚话、发牢骚、记录灵感的地方。" />

## ✍️ My Space

{teamMembers.map((member, index) => (
  <>
    <MySpace
      key={member.id}
      name={member.name}
      avatar={member.avatar}
      emoji={member.spaceEmoji}
      content={member.spaceContent}
    />
    {index < teamMembers.length - 1 && <hr />}
  </>
))}

---

# 🎉 结束语  
如果你能看到这里——  

**说明你是真爱，不是来看我写骚话的。**

> 文档写得越好，加班越少。  
> 团队越骚，文化越强。  

欢迎继续扩展、改造、玩出花来！
