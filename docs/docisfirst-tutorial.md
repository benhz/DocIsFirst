---
sidebar_position: 1
title: DocIsFirst 使用教程
---

# DocIsFirst 使用教程

欢迎使用 DocIsFirst！这是一个基于 Docusaurus 构建的文档管理平台。本教程将教你如何快速上手。

## 项目简介

DocIsFirst 是一个功能强大的文档站点，具有以下特点：

- 📚 多文档实例支持
- 📝 博客系统
- 🎨 主题定制
- 🌏 中文支持
- ⚡ React 驱动

## 一、如何添加新的导航 Tab

### 场景：在"博客"后面新增"开发" Tab

#### 步骤 1：创建文档目录

在项目根目录创建新的文档文件夹：

```bash
mkdir development
```

#### 步骤 2：创建侧边栏配置

创建 `sidebarsDevelopment.ts` 文件：

```typescript
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  developmentSidebar: [
    {
      type: 'autogenerated',
      dirName: '.',
    },
  ],
};

export default sidebars;
```

#### 步骤 3：配置文档插件

编辑 `docusaurus.config.ts`，在 `plugins` 数组中添加：

```typescript
plugins: [
  // 已有的插件...
  [
    '@docusaurus/plugin-content-docs',
    {
      id: 'development',
      path: 'development',
      routeBasePath: 'development',
      sidebarPath: './sidebarsDevelopment.ts',
    },
  ],
],
```

#### 步骤 4：添加导航栏入口

继续编辑 `docusaurus.config.ts`，在 `navbar.items` 中添加：

```typescript
navbar: {
  items: [
    // 已有的导航项...
    {to: '/blog', label: '博客', position: 'left'},
    // 新增开发 Tab
    {
      type: 'docSidebar',
      sidebarId: 'developmentSidebar',
      position: 'left',
      label: '开发',
      docsPluginId: 'development',
    },
  ],
}
```

#### 步骤 5：创建入口文档

在 `development` 目录下创建 `intro.md`：

```markdown
---
sidebar_position: 1
title: 开发文档
---

# 开发文档

欢迎来到开发文档区域！
```

#### 步骤 6：启动查看效果

```bash
npm run start
```

导航栏中会出现新的"开发" Tab！

## 二、如何在 Tab 下新增文档

### 方式 1：创建单个文档

直接在文档目录下创建 Markdown 文件：

```markdown
---
sidebar_position: 2
title: API 指南
---

# API 开发指南

这是 API 开发的详细说明。
```

**配置说明：**
- `sidebar_position`：控制显示顺序，数字越小越靠前
- `title`：文档标题

### 方式 2：创建文档分类

#### 1. 创建分类目录

```bash
mkdir development/backend
```

#### 2. 配置分类信息

创建 `development/backend/_category_.json`：

```json
{
  "label": "后端开发",
  "position": 3,
  "link": {
    "type": "generated-index",
    "description": "后端开发相关文档"
  }
}
```

#### 3. 在分类下添加文档

创建 `development/backend/nodejs.md`：

```markdown
---
sidebar_position: 1
---

# Node.js 开发

Node.js 开发指南内容...
```

### 文档组织示例

```
development/
├── intro.md              (首页)
├── api-guide.md         (API指南)
├── backend/             (后端分类)
│   ├── _category_.json
│   ├── nodejs.md
│   └── database.md
└── frontend/            (前端分类)
    ├── _category_.json
    ├── react.md
    └── styling.md
```

侧边栏将显示为：

```
📘 开发文档
📄 API 开发指南
📁 后端开发
  └─ Node.js 开发
  └─ 数据库设计
📁 前端开发
  └─ React 开发
  └─ 样式开发
```

## 三、Markdown 基础功能

### 标题和段落

```markdown
# 一级标题
## 二级标题
### 三级标题

这是一个段落。

这是另一个段落。
```

### 列表

```markdown
无序列表：
- 项目 1
- 项目 2
- 项目 3

有序列表：
1. 第一项
2. 第二项
3. 第三项
```

### 代码块

使用三个反引号创建代码块：

````markdown
```javascript
function hello() {
  console.log("Hello World");
}
```
````

### 链接和图片

```markdown
[链接文本](https://example.com)

![图片描述](/img/screenshot.png)
```

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 值1 | 值2 | 值3 |
| 值4 | 值5 | 值6 |
```

## 四、Docusaurus 特殊功能

### 提示框

```markdown
:::note
这是一个注意事项。
:::

:::tip
这是一个小贴士！
:::

:::info
这是一些额外信息。
:::

:::caution
请谨慎操作。
:::

:::danger
这个操作不可逆！
:::
```

效果：

:::note
这是一个注意事项。
:::

:::tip
这是一个小贴士！
:::

### 代码块标题和高亮

````markdown
```javascript title="app.js"
function hello() {
  console.log("Hello World");
}
```
````

## 五、博客管理

### 创建博客文章

在 `blog` 目录下创建文件，使用日期命名：

```markdown
---
slug: my-first-post
title: 我的第一篇博客
authors: [你的名字]
tags: [hello, docusaurus]
---

这是博客摘要。

<!--truncate-->

这里是文章正文内容。
```

### 配置作者

编辑 `blog/authors.yml`：

```yaml
yourname:
  name: 你的名字
  title: 开发者
  url: https://github.com/yourname
  image_url: https://github.com/yourname.png
```

### 配置标签

编辑 `blog/tags.yml`：

```yaml
hello:
  label: 你好
  permalink: /hello
  description: 欢迎相关文章
```

## 六、常用命令

### 开发调试

```bash
# 启动开发服务器
npm run start

# 构建生产版本
npm run build

# 预览生产版本
npm run serve
```

### 清理缓存

```bash
# 清除缓存
npm run clear

# 清除并重新构建
npm run clear && npm run build
```

## 七、目录结构说明

```
DocIsFirst/
├── docs/                 # 主文档目录
├── blog/                 # 博客目录
├── projects/             # 项目文档（示例）
├── development/          # 开发文档（示例）
├── src/                  # React 组件
│   ├── components/
│   ├── css/
│   └── pages/
├── static/               # 静态资源
│   └── img/
├── docusaurus.config.ts  # 主配置文件
├── sidebars.ts          # 文档侧边栏配置
└── package.json
```

## 八、配置文件说明

### docusaurus.config.ts

核心配置文件，包含：
- 站点基本信息（title, tagline, url）
- 插件配置（plugins）
- 主题配置（themeConfig）
- 导航栏配置（navbar）
- 页脚配置（footer）

### sidebars.ts

侧边栏配置文件，定义文档的组织结构。可以使用：
- `autogenerated`：自动根据文件结构生成
- 手动配置：精确控制每个文档的位置

## 九、最佳实践

### 文档编写建议

1. **使用清晰的标题**：便于导航和搜索
2. **添加代码示例**：帮助读者理解
3. **使用提示框**：突出重要信息
4. **保持简洁**：避免冗长的段落
5. **定期更新**：保持文档与代码同步

### 文件组织建议

1. **按功能分类**：相关文档放在同一目录
2. **使用有意义的文件名**：便于识别
3. **设置合理的 position**：重要文档靠前
4. **添加分类描述**：使用 `_category_.json`

### 性能优化

1. **图片优化**：压缩图片大小
2. **避免大文件**：将长文档拆分
3. **使用 CDN**：加速静态资源加载

## 十、故障排查

### 常见问题

**1. 侧边栏不显示**

检查：
- `sidebarId` 是否匹配
- `docsPluginId` 是否正确
- 文档是否存在

**2. 构建失败**

检查：
- Node.js 版本（需要 18+）
- 依赖是否完整安装
- Markdown 语法是否正确

**3. 图片 404**

检查：
- 路径是否以 `/` 开头
- 文件是否在 `static` 目录
- 文件名大小写是否匹配

**4. 端口被占用**

```bash
# 指定其他端口
npm run start -- --port 3001
```

## 十一、快速参考

### 添加 Tab 步骤总结

1. 创建文档目录
2. 创建侧边栏配置文件
3. 在 `docusaurus.config.ts` 添加插件
4. 在 `navbar.items` 添加导航项
5. 创建入口文档
6. 重启开发服务器

### 添加文档步骤总结

1. 在对应目录创建 `.md` 文件
2. 添加 Front Matter（`sidebar_position`、`title`）
3. 编写文档内容
4. 保存后自动更新

## 十二、获取帮助

- 📖 [Docusaurus 官方文档](https://docusaurus.io/zh-CN/)
- 💬 [GitHub Discussions](https://github.com/facebook/docusaurus/discussions)
- 🐛 [问题反馈](https://github.com/facebook/docusaurus/issues)

## 总结

通过本教程，你已经学会了：

✅ 如何添加新的导航 Tab
✅ 如何创建和组织文档
✅ Markdown 和 Docusaurus 特殊语法
✅ 博客管理
✅ 常用命令和最佳实践

现在开始编写你的文档吧！📝
