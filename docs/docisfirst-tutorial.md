---
sidebar_position: 1
title: DocIsFirst 使用教程
---

# DocIsFirst 使用教程

欢迎使用 DocIsFirst！这是一个基于 Docusaurus 3.9.2 构建的现代化文档管理平台。本教程将帮助你快速掌握如何管理和组织你的文档内容。

## 项目简介

DocIsFirst（Qicat）是一个功能强大的文档站点，具有以下特点：

- 📚 **多文档实例支持**：可以创建多个独立的文档区域
- 📝 **博客系统**：内置完整的博客功能，支持标签、作者、RSS订阅
- 🎨 **主题定制**：支持亮色/暗色模式，自动跟随系统偏好
- 🌏 **国际化支持**：当前配置为中文，可扩展多语言
- ⚡ **React驱动**：基于现代化的React 19技术栈

## 一、如何添加新的导航Tab

### 场景示例：在"博客"后面新增"开发"Tab

假设你想在导航栏中添加一个新的"开发"tab，展示开发相关的文档。以下是完整步骤：

### 步骤1：创建文档目录

在项目根目录创建新的文档目录：

```bash
mkdir development
```

### 步骤2：配置Docusaurus插件

编辑 `docusaurus.config.ts` 文件，在 `plugins` 数组中添加新的文档插件实例：

```typescript title="docusaurus.config.ts"
plugins: [
  // 现有的 projects 插件
  [
    '@docusaurus/plugin-content-docs',
    {
      id: 'projects',
      path: 'projects',
      routeBasePath: 'projects',
      sidebarPath: './sidebarsProjects.ts',
    },
  ],
  // 新增：开发文档插件
  [
    '@docusaurus/plugin-content-docs',
    {
      id: 'development',           // 唯一ID
      path: 'development',         // 文档目录路径
      routeBasePath: 'development', // URL路径
      sidebarPath: './sidebarsDevelopment.ts', // 侧边栏配置
    },
  ],
],
```

### 步骤3：创建侧边栏配置文件

在项目根目录创建 `sidebarsDevelopment.ts`：

```typescript title="sidebarsDevelopment.ts"
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  developmentSidebar: [
    {
      type: 'autogenerated',
      dirName: '.', // 自动从 development 目录生成
    },
  ],
};

export default sidebars;
```

### 步骤4：在导航栏添加Tab

继续编辑 `docusaurus.config.ts`，在 `navbar.items` 数组中添加新的导航项：

```typescript title="docusaurus.config.ts"
navbar: {
  title: 'Qicat',
  logo: {
    alt: 'Qicat Logo',
    src: 'img/logo.svg',
  },
  items: [
    {
      type: 'docSidebar',
      sidebarId: 'tutorialSidebar',
      position: 'left',
      label: '教程',
    },
    {
      type: 'docSidebar',
      sidebarId: 'projectsSidebar',
      position: 'left',
      label: '项目',
      docsPluginId: 'projects',
    },
    {to: '/blog', label: '博客', position: 'left'},
    // 新增：开发Tab
    {
      type: 'docSidebar',
      sidebarId: 'developmentSidebar',  // 对应侧边栏ID
      position: 'left',
      label: '开发',                     // 显示文本
      docsPluginId: 'development',       // 对应插件ID
    },
    {
      href: 'https://github.com/facebook/docusaurus',
      label: 'GitHub',
      position: 'right',
    },
  ],
}
```

### 步骤5：创建首页文档

在 `development` 目录下创建入口文档：

```markdown title="development/intro.md"
---
sidebar_position: 1
title: 开发文档
---

# 开发文档

这里是开发相关的文档内容。

## 快速开始

欢迎来到开发文档区域！
```

### 完成！

重启开发服务器，你将在导航栏看到新的"开发"tab：

```bash
npm run start
```

## 二、如何在Tab下新增文档

在已有的Tab下添加新文档非常简单，有两种主要方式：

### 方式1：单个文档文件

直接在文档目录下创建 Markdown 文件：

```markdown title="development/api-guide.md"
---
sidebar_position: 2
title: API 开发指南
---

# API 开发指南

这是API开发的详细说明。

## RESTful API设计

...
```

**关键配置说明**：
- `sidebar_position`: 控制在侧边栏中的显示顺序（数字越小越靠前）
- `title`: 文档标题，会显示在浏览器标签和侧边栏

### 方式2：文档分类目录

创建文档分类，将相关文档组织在一起：

#### 步骤1：创建分类目录

```bash
mkdir development/backend
```

#### 步骤2：配置分类信息

创建 `_category_.json` 文件：

```json title="development/backend/_category_.json"
{
  "label": "后端开发",
  "position": 3,
  "link": {
    "type": "generated-index",
    "description": "后端开发相关文档汇总"
  }
}
```

**配置说明**：
- `label`: 分类显示名称
- `position`: 在侧边栏中的位置
- `link.type`: `generated-index` 会自动生成该分类的索引页
- `description`: 分类描述

#### 步骤3：在分类下添加文档

```markdown title="development/backend/nodejs-guide.md"
---
sidebar_position: 1
---

# Node.js 开发指南

Node.js相关的开发文档。

## 环境配置

...
```

```markdown title="development/backend/database.md"
---
sidebar_position: 2
---

# 数据库设计

数据库设计最佳实践。

## Schema设计

...
```

### 文档组织结构示例

```
development/
├── intro.md                    # 首页（position: 1）
├── api-guide.md               # API指南（position: 2）
├── backend/                   # 后端分类（position: 3）
│   ├── _category_.json
│   ├── nodejs-guide.md       # Node.js指南
│   └── database.md           # 数据库设计
└── frontend/                  # 前端分类（position: 4）
    ├── _category_.json
    ├── react-guide.md        # React指南
    └── styling.md            # 样式开发
```

这样的结构会在侧边栏生成：
```
📘 开发文档
📄 API 开发指南
📁 后端开发
  └─ Node.js 开发指南
  └─ 数据库设计
📁 前端开发
  └─ React 指南
  └─ 样式开发
```

## 三、进阶功能

### 3.1 Markdown 扩展功能

DocIsFirst 支持丰富的 Markdown 扩展，让你的文档更加生动：

#### 提示框（Admonitions）

```markdown
:::note 注意
这是一个注意事项。
:::

:::tip 小贴士
使用这个功能可以提高效率！
:::

:::info 信息
这是一些额外信息。
:::

:::caution 警告
请谨慎操作此步骤。
:::

:::danger 危险
这个操作不可逆！
:::
```

效果：

:::tip 小贴士
使用这个功能可以提高效率！
:::

#### 代码块高亮

````markdown
```javascript title="src/components/Button.js"
function Button({label, onClick}) {
  // highlight-start
  const handleClick = () => {
    console.log('Button clicked');
    onClick();
  };
  // highlight-end

  return <button onClick={handleClick}>{label}</button>;
}
```
````

#### 选项卡（Tabs）

需要使用 MDX 格式（`.mdx` 文件）：

```mdx
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="npm" label="npm" default>
    ```bash
    npm install docusaurus
    ```
  </TabItem>
  <TabItem value="yarn" label="Yarn">
    ```bash
    yarn add docusaurus
    ```
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    ```bash
    pnpm add docusaurus
    ```
  </TabItem>
</Tabs>
```

### 3.2 博客文章管理

#### 创建博客文章

在 `blog` 目录下创建文件，使用日期命名：

```markdown title="blog/2024-12-03-my-first-post.md"
---
slug: my-first-post
title: 我的第一篇博客
authors: [qicat]
tags: [hello, docusaurus]
---

这是博客摘要，会显示在列表页。

<!--truncate-->

这里是文章正文内容，只在详情页显示。

## 章节1

...
```

#### 配置作者信息

编辑 `blog/authors.yml`：

```yaml
qicat:
  name: Qicat
  title: 技术探索者
  url: https://github.com/qicat
  image_url: https://github.com/qicat.png
  socials:
    github: qicat
```

#### 配置标签

编辑 `blog/tags.yml`：

```yaml
docusaurus:
  label: Docusaurus
  permalink: /docusaurus
  description: Docusaurus相关文章

frontend:
  label: 前端开发
  permalink: /frontend
  description: 前端技术文章
```

### 3.3 自定义页面

在 `src/pages` 目录下创建自定义页面：

```tsx title="src/pages/about.tsx"
import React from 'react';
import Layout from '@theme/Layout';

export default function About() {
  return (
    <Layout title="关于我们" description="关于我们的页面">
      <div style={{padding: '2rem'}}>
        <h1>关于我们</h1>
        <p>这是一个自定义页面示例。</p>
      </div>
    </Layout>
  );
}
```

访问地址：`http://localhost:3000/about`

也可以使用 Markdown 创建：

```markdown title="src/pages/contact.md"
# 联系我们

- Email: contact@example.com
- GitHub: https://github.com/qicat
```

### 3.4 文档版本管理

Docusaurus 支持文档版本控制：

```bash
# 创建新版本
npm run docusaurus docs:version 1.0.0
```

这会：
1. 在 `versioned_docs/version-1.0.0/` 创建当前文档快照
2. 在 `versioned_sidebars/` 创建侧边栏快照
3. 更新 `versions.json`

### 3.5 搜索功能

DocIsFirst 可以集成搜索插件：

#### 本地搜索

安装插件：

```bash
npm install --save @easyops-cn/docusaurus-search-local
```

配置：

```typescript title="docusaurus.config.ts"
themes: [
  [
    require.resolve("@easyops-cn/docusaurus-search-local"),
    {
      hashed: true,
      language: ["zh"],
      highlightSearchTermsOnTargetPage: true,
    },
  ],
],
```

#### Algolia 搜索

```typescript title="docusaurus.config.ts"
themeConfig: {
  algolia: {
    appId: 'YOUR_APP_ID',
    apiKey: 'YOUR_SEARCH_API_KEY',
    indexName: 'YOUR_INDEX_NAME',
  },
}
```

### 3.6 自定义样式

编辑 `src/css/custom.css` 修改主题色：

```css title="src/css/custom.css"
:root {
  --ifm-color-primary: #2e8555;
  --ifm-color-primary-dark: #29784c;
  --ifm-color-primary-darker: #277148;
  --ifm-color-primary-darkest: #205d3b;
  --ifm-color-primary-light: #33925d;
  --ifm-color-primary-lighter: #359962;
  --ifm-color-primary-lightest: #3cad6e;
}

[data-theme='dark'] {
  --ifm-color-primary: #25c2a0;
  --ifm-color-primary-dark: #21af90;
  --ifm-color-primary-darker: #1fa588;
  --ifm-color-primary-darkest: #1a8870;
  --ifm-color-primary-light: #29d5b0;
  --ifm-color-primary-lighter: #32d8b4;
  --ifm-color-primary-lightest: #4fddbf;
}
```

### 3.7 静态资源管理

将图片、PDF等静态文件放在 `static` 目录：

```
static/
├── img/
│   ├── logo.svg
│   └── screenshots/
│       └── demo.png
└── files/
    └── manual.pdf
```

在文档中引用：

```markdown
![截图](/img/screenshots/demo.png)

[下载用户手册](/files/manual.pdf)
```

### 3.8 多语言支持

配置多语言：

```typescript title="docusaurus.config.ts"
i18n: {
  defaultLocale: 'zh-Hans',
  locales: ['zh-Hans', 'en'],
  localeConfigs: {
    'zh-Hans': {
      label: '简体中文',
    },
    en: {
      label: 'English',
    },
  },
},
```

创建翻译文件：

```bash
npm run write-translations -- --locale en
```

## 四、常用命令

### 开发

```bash
# 启动开发服务器
npm run start

# 构建生产版本
npm run build

# 本地预览生产版本
npm run serve
```

### 部署

```bash
# 部署到 GitHub Pages
npm run deploy

# 使用 Docker
docker build -t docisfirst .
docker run -p 80:80 docisfirst

# 使用 Kubernetes
kubectl apply -f k8s/
```

### 维护

```bash
# 清除缓存
npm run clear

# 更新依赖
npm update

# 检查链接
npm run build && npx broken-link-checker http://localhost:3000
```

## 五、最佳实践

### 文档组织建议

1. **使用清晰的目录结构**：按功能或主题分类
2. **合理设置 position**：重要文档靠前
3. **编写有意义的标题**：便于搜索和导航
4. **添加适当的元数据**：description、keywords等

### 写作技巧

1. **使用提示框**：突出重要信息
2. **代码示例要完整**：包含上下文
3. **添加图片说明**：用alt属性描述
4. **保持一致性**：术语、格式、风格

### 性能优化

1. **图片优化**：压缩大小，使用WebP格式
2. **懒加载**：大型组件使用动态导入
3. **缓存策略**：合理配置浏览器缓存

### SEO优化

1. **设置页面描述**：每个文档添加description
2. **使用语义化标题**：h1-h6层级清晰
3. **添加结构化数据**：帮助搜索引擎理解内容

## 六、故障排查

### 常见问题

#### 1. 侧边栏不显示

检查：
- `sidebarId` 是否匹配
- `docsPluginId` 是否正确
- 文档目录是否有内容

#### 2. 样式不生效

检查：
- CSS文件是否正确导入
- 浏览器缓存是否清除
- CSS类名是否正确

#### 3. 构建失败

检查：
- Node.js版本（需要18+）
- 依赖是否完整安装
- 文档格式是否正确

#### 4. 图片显示404

检查：
- 路径是否正确（以`/`开头）
- 文件是否在`static`目录
- 文件名大小写是否匹配

## 七、获取帮助

- 📖 [Docusaurus 官方文档](https://docusaurus.io/zh-CN/)
- 💬 [GitHub Discussions](https://github.com/facebook/docusaurus/discussions)
- 🐛 [问题反馈](https://github.com/facebook/docusaurus/issues)
- 💡 [Stack Overflow](https://stackoverflow.com/questions/tagged/docusaurus)

## 总结

通过本教程，你已经学会了：

✅ 如何在导航栏添加新的Tab
✅ 如何在Tab下创建和组织文档
✅ 如何使用Markdown扩展功能
✅ 如何管理博客文章
✅ 如何自定义主题和样式
✅ 如何优化和部署文档站点

现在，开始你的文档编写之旅吧！记住，好的文档是项目成功的关键。

---

**Happy Documenting! 📝**
